name: TODO to GitHub Issues

on:
  push:
    branches:
      - main

jobs:
  todo:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Create issues from TODOs
        uses: JasonEtco/todo@v1
        with:
          file: TODO.md
